DB:
    *Create Historical_PlayerToTeams Table
Classes:
    *Create DBConnection Class:
        *SavePlayer() == DONE
        *loadPlayer()
        *updatePlayer()
        *deleterPlayer()
        *loadPlayers()
        *loadTeams()
Form:
    *Create and link grid to DB
    *Validation