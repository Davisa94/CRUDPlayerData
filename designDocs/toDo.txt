DB:
    *Create HistoricalPlayerToTeams Table
    *Create PlayerToTeam Table
    ~Create Enumeration Team Table and convert team entry to dropdown
Classes:
    *Create DBConnection Class:
        *SavePlayer() == DONE
            *AddPlayerToTeam()
            *AddPlayerToHistoricalTeams()
        *loadPlayer()
        *updatePlayer()
        *deleterPlayer()
        *loadPlayers()
        *loadTeams() DONE
Form:
    *Create and link grid to DB
    *Validation
    *Double Check Tabbing